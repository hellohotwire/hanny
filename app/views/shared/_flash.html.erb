<% if notice %>
  <div class="bg-gradient-to-br from-sky-700 to-sky-600 text-white text-center py-2 z-50 relative" role="alert">
    <p><%= sanitize notice %></p>
  </div>
<% end %>

<% if alert %>
  <div class="bg-gradient-to-br from-red-600 to-red-400 text-white text-center py-2 z-50 relative" role="alert">
    <p><%= sanitize alert %></p>
  </div>
<% end %>
